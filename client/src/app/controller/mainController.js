import { addUser } from '../actions/main-action';
const registerControl = (emailOrPhone, nameSurname, userName, userPassword) => {
    return dispatch => {
        
    };
}

export { registerControl };